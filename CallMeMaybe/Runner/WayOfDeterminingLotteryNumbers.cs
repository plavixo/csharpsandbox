namespace Runner
{
    delegate List<int> WayOfDeterminingLotteryNumbers();

    /*
     * Notice that we declare a delegate at the same level as we declare a class or enum etc;
     * it's that kind of level of thing.
     *
     * Notice that the delegate has no arguments in the parentheses, and returns a List<int>,
     * just like the methods in Amanda, Dorothy, and co.
     */


    class UnrelatedClassThatIsJustHereAsAnExample
    {
    }

    enum ExampleEnum
    {
        Unrelated,
        Enum,
        Here,
        As,
        An,
        Example
    }
}